# Horizon API — HTTPS, no trailing slash.
# Vercel: Project → Settings → Environment Variables → VITE_API_BASE_URL (Production + Preview).
VITE_API_BASE_URL=https://horizon-backend-4e0i.onrender.com
# Local dev:
# VITE_API_BASE_URL=http://127.0.0.1:3000

# Optional: client-side PDF size ceiling before uploading (defaults to ~25MiB)
# VITE_MAX_ADMIN_PDF_BYTES=26214400
